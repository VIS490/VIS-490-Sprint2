import React from 'react'
import Typography from '@material-ui/core/Typography';

const Quiz = () => {
    return (
        <div className="Quiz">
            Quiz.js
        </div>
    );
}

export default Quiz;